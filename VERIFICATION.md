# Verification

Run these checks to prove the mobile prototype is in a healthy state:

```bash
cd D:/morgan/10_Projects/xalmg-til-app
npx tsc --noEmit
npx expo-doctor
```

Current local status: TypeScript passes and Expo Doctor reports 17/17 checks.
